//'use strict';

//describe('Controller: UsersCtrl', function () {

  //// load the controller's module
  //beforeEach(module('buildpayApp'));

  //var Users = jasmine.createSpyObj('Users', ['query']);

  //var users = jasmine.createSpy('mockusers');

  //var usersCtrl,
    //scope;

  //beforeEach(inject(function ($controller, $rootScope, $q) {
    //scope = $rootScope.$new();

    //Users.query.and.returnValue($q.when({ data: { users: users } }));

    //usersCtrl = $controller('UsersCtrl', { Users: Users });
  //}));

  //it('gets users from api', function () {
    //// initial value
    //expect(usersCtrl.users).toEqual([]);

    //// after retrieving users from service
    //scope.$digest();
    //expect(usersCtrl.users).toEqual(users);
  //});
//});
