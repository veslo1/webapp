'use strict';

//describe('Controller: MyProfileCtrl', function () {
  //beforeEach(module('buildpayApp'));

  //var MyProfileCtrl, scope, Messages;

  //var decoratedProfiles = jasmine.createSpyObj('profilesSpy', ['get', 'update']);

  //beforeEach(inject(function ($controller, $rootScope, $q) {
    //scope = $rootScope.$new();

    //Messages = jasmine.createSpyObj('Messages', ['addSuccess']);

    //decoratedProfiles.get.and.returnValue($q.when({ data: { profile: 'profilestuff' } }));
    //decoratedProfiles.update.and.returnValue($q.when({ data: { success: true }}));

    //MyProfileCtrl = $controller('MyProfileCtrl', {
      //Profiles: decoratedProfiles,
      //Messages: Messages
    //});
  //}));

  //it('initializes profile', function() {
    //scope.$digest();

    //expect(MyProfileCtrl.profile).toEqual('profilestuff');
  //});

  //describe('updateProfile', function() {
    //it('utilizes Profile to update profile data', function() {
      //MyProfileCtrl.updateProfile();

      //scope.$digest();

      //expect(Messages.addSuccess).toHaveBeenCalledWith('Profile updated.');
      //expect(decoratedProfiles.update).toHaveBeenCalled();
    //});
  //});
//});
