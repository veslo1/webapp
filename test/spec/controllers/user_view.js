//'use strict';

//describe('Controller: UserViewCtrl', function () {

  //// load the controller's module
  //beforeEach(module('buildpayApp'));

  //var UserViewCtrl, Messages;

  //var user = {
    //id: 123,
    //full_name: 'daniel errante'
  //};

  //beforeEach(inject(function ($controller, $rootScope) {
    //Messages = jasmine.createSpyObj('Messages', ['addSuccess']);

    //UserViewCtrl = $controller('UserViewCtrl', {
      //user: user,
      //Messages: Messages
    //});
  //}));


  //it('sets project to response from api', function () {
    //expect(UserViewCtrl.user).toEqual(user);
  //});
//});
