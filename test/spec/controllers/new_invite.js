//'use strict';

//describe('Controller: NewInviteCtrl', function () {

  //// load the controller's module
  //beforeEach(module('buildpayApp'));

  //var newInviteCtrl,
    //scope, state, Messages;

  //var Invites = jasmine.createSpyObj('Invites', ['createInvite']);

  //var invite = {
    //first_name: '',
    //last_name: '',
    //email: ''
  //};

  //beforeEach(inject(function ($controller, $rootScope, $q) {
    //scope = $rootScope.$new();

    //state = jasmine.createSpyObj('stateSpy', ['go']);
    //Messages = jasmine.createSpyObj('Messages', ['addSuccess']);

    //Invites.createInvite.and.returnValue($q.when({}));

    //newInviteCtrl = $controller('NewInviteCtrl', {
      //Invites: Invites,
      //$state: state,
      //Messages: Messages
    //});
  //}));

  //it('initializes invite with empty data', function () {
    //expect(newInviteCtrl.invite).toEqual(invite);
  //});

  //describe('createInvite', function() {
    //it('sends invite to invites api on submit', function() {
      //newInviteCtrl.createInvite();
      //scope.$digest();
      //expect(Invites.createInvite).toHaveBeenCalledWith(invite);
      //expect(Messages.addSuccess).toHaveBeenCalledWith('Invite sent.');
      //expect(state.go).toHaveBeenCalledWith('directory.users');
    //});
  //});
//});
