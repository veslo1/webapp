'use strict';

//describe('Controller: ProjectFundsCtrl', function () {
  //beforeEach(module('buildpayApp'));

  //var ProjectFundsCtrl, scope, Messages, projectPermissionsService;

  //var projectId = 12345;

  //var stateParams = {
    //id: projectId
  //};

  //beforeEach(inject(function ($controller, $rootScope, $q) {
    //scope = $rootScope.$new();

    //Messages = jasmine.createSpyObj('Messages', ['addSuccess']);

    //projectPermissionsService = jasmine.createSpyObj('projectPermissionsService', ['getProjectPermissions']);
    //projectPermissionsService.getProjectPermissions.and.returnValue($q.when({ data: { permissions: { can_apply_project_funds: true, can_view_project_funds: true} } }));

    //ProjectFundsCtrl = $controller('ProjectFundsCtrl', {
      //$stateParams: stateParams,
      //Messages: Messages,
      //projectPermissionsService: projectPermissionsService
    //});
  //}));
//});
