//'use strict';

//describe('Controller: NewProjectCtrl', function () {

  //// load the controller's module
  //beforeEach(module('buildpayApp'));

  //var newProjectCtrl,
    //scope, state;

  //var Projects = jasmine.createSpyObj('Projects', ['createProject']);

  //var project = {
    //name: '',
    //address_1: '',
    //address_2: '',
    //city: '',
    //state: '',
    //postal_code: '',
    //claim_number: '',
    //description: '',
    //comments: '',
    //material_funds: '',
    //labor_funds: '',
    //overhead_funds: '',
    //profit_funds: ''
  //};

  //// Initialize the controller and a mock scope
  //beforeEach(inject(function ($controller, $rootScope, $q) {
    //scope = $rootScope.$new();

    //state = jasmine.createSpyObj('state', ['go']);

    //Projects.createProject.and.returnValue($q.when({ project: { name: 'some project' } }));

    //newProjectCtrl = $controller('NewProjectCtrl', {
      //Projects: Projects,
      //$state: state
    //});
  //}));

  //it('initializes project with empty data', function () {
    //expect(newProjectCtrl.project).toEqual(project);
  //});

  //describe('createProject', function() {
    //it('sends project to projects api on submit', function() {
      //newProjectCtrl.createProject();
      //scope.$digest();
      //expect(Projects.createProject).toHaveBeenCalledWith(project);
      //expect(state.go).toHaveBeenCalledWith('projects');
    //});
  //});
//});
