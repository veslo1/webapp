'use strict';

describe('Directive: fieldErrors', function () {

  // load the directive's module
  beforeEach(module('buildpayApp'));

  var element, scope, attrs, ctrl;

  beforeEach(inject(function ($rootScope) {
    //scope = $rootScope.$new();
  }));

  it('should make hidden element visible', inject(function ($compile) {
    //element = angular.element('<field-errors></field-errors>');
    //element = $compile(element)(scope);
    //expect(element.text()).toBe('this is the fieldErrors directive');
  }));
});
